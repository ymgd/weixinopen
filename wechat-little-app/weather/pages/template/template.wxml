<template name="future-item">
   <view class="future-item">
      <view>{{item.date}}</view>
      <view>{{item.type}}</view>
      <view>{{item.fengxiang}} {{item.fengli}}</view>
      <view>{{item.low}}</view>
      <view>{{item.high}}</view>

   </view>
</template>