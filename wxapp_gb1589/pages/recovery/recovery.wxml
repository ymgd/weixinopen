<page>
    <view class="review-history-list">
        <view wx:for="{{list}}">
            <image src="{{item.imgurl}}" mode="aspectFill" />
            <text>{{item.caption}}</text>
        </view>
    </view>
</page>
